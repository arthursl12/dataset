df = pd.read_sql_query("SELECT DISTINCT tipo_evento FROM mytable", conn)
df

df = pd.read_sql_query("SELECT tipo_evento FROM mytable WHERE tipo_evento NOT NULL", conn)
set_ = {""}
corte = 0
for k in range(df.size):
  string = df['tipo_evento'].values[k]
  for i in range (len(string)):
      if (string[i] == ',' or string[i] == ';'):
        palavra = string[corte:i]
        set_.add(palavra)
        i+=1
        corte = i
  palavra = string[corte:]
  corte = 0;
  set_.add(palavra)

set_.remove("")
set_

df = pd.DataFrame(set_)
cursor.execute("DROP TABLE Evento")
cursor.execute("DROP TABLE Evento_temp")
df.to_sql(name="Evento", con=conn)
df_sql = pd.read_sql('select * from Evento', conn)

cursor.execute("ALTER TABLE Evento RENAME TO Evento_temp")
cursor.execute("CREATE TABLE Evento (id_Evento INTEGER PRIMARY KEY, Evento VARCHAR);")
cursor.execute("INSERT INTO Evento (id_Evento, Evento) SELECT * FROM Evento_temp")
df_evento = pd.read_sql_query("SELECT * FROM Evento", conn)

df_evento
